"use strict";       //it treats all JS code as newer version

// alert(4+32)      //WE are using nodejs, not browser

console.log(3 + 3);     // code readibilty should be your First priority
                        //its your choice is u can skip also ;(semicolon);

let name = "Junaid"
let age=19
let isLoggedIn = false

//different data types

//1.number => 2 to power 53
//2.bigint => used to write big numbers
//3.boolean => true/false
//4.null =>its stand alone value and datatype in JS it can be considerd as empty
//5. undefined => it means its store in memory but not defined yet 
//6.symbol => it used to find unique value
//7.object => null is also called object


console.log(typeof undefined) //type is undefined
console.log(typeof null) //type is object
