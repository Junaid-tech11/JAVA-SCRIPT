# Javascript and Classes

does js has classes it can be no / yes.
classes include in es6 and beside that JS is proto-type-based language
classes are primarily syntactic sugar over.
it is prototype based language under the hood

# OOP

OOP is paradigm:it is the way to how to write the code

# OBject

-collection of propertied and methods
-toLowerCase, promises, new keyword this all are objects

# why use OOp

-code become messy so oop help us to write clean code

# parts of OOp

-object literal{}  
-constructor function
-prototypes
-classes
-Instances(new, this)

# 4 pillars of OOP

Abstraction(Details hide , fetch())
Encapsulation(Data coupling(wrapper))
Inheritance(derived classes)
Polymorphism()
